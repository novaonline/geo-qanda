import { OwnersActionFormatterPipe } from './owners-action-formatter.pipe';

describe('OwnersActionFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new OwnersActionFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
