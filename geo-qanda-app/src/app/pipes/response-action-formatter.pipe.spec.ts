import { ResponseActionFormatterPipe } from './response-action-formatter.pipe';

describe('ResponseActionFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new ResponseActionFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
